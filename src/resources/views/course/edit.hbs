<h1 class="text-primary"> khóa học {{course.name}}</h1>
<form action="/course/{{course._id}}?_method=PUT" method="POST">
    <div class="form-group">
      <label for="name">Tên Khóa Học</label>
      <input type="text" name="name" id="" class="form-control" placeholder="" value={{course.name}} >
    </div>
    <div class="form-group">
      <label for="description">Mô tả</label>
      <textarea name="description" id="description" cols="150">{{course.description}}</textarea>
    </div>
      <div class="form-group">
      <label for="id">ID img</label>
      <input type="text" name="id" id="" class="form-control" placeholder="" value={{course.id}} >
    </div>
    <button type="submit" class="btn btn-primary">Cập nhật</button>
</form>